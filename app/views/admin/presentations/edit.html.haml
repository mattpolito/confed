%h1 Editing #{@presentation.title}

= form_for [:admin, @presentation] do |f|
  = render 'form', :f => f, 
    :presentation => @presentation, 
    :speakers     => @speakers, 
    :events       => @events

= link_to 'Show', @presentation
\|
= link_to 'Back', admin_speakers_path
