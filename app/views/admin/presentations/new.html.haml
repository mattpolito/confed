%h2 New Presentation

= form_for [:admin, @presentation] do |f|
  = render 'form', :f => f,
    :presentation => @presentation, 
    :speakers     => @speakers, 
    :events       => @events

%p.actions
  = link_to 'Cancel & Go Back', :back
