= form_for [:admin, @presentation] do |f|
  = render 'form', :f => f, :presentation => @presentation, 
    :presentation => @presentation, 
    :speakers     => @speakers, 
    :events       => @events