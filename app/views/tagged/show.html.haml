- meta_content_for(:title) { "Feed me some: #{@tag.name}" }

%fieldset
  %legend Feed me some: #{@tag.name}

= render :partial => 'presentations/presentation', :collection => @presentations

= will_paginate @presentations
